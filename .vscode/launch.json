{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "启动调试",
            "type": "cortex-debug",
            "request": "launch",
            "executable": "build/arm-none-eabi/zlcr_revc.elf",
            "cwd": "${workspaceFolder}",
            "gdbTarget": "localhost:3333",
            "servertype": "external",
            "preLaunchTask": "build"
        }
    ]
}